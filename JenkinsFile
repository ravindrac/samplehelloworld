pipeline {
    agent any

    stages {
        stage('Build') {
            ant compile jar run
            }
        }        
    }
	post {
        always {
            archive ‘build/jar/*.jar’
        }
	}
	
}